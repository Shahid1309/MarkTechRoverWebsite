import React from 'react';
import { Box, Typography } from '@mui/material';

const ProposalBuilder: React.FC = () => (
  <Box p={4} textAlign="center">
    <Typography variant="h5">Proposal Builder</Typography>
    <Typography color="text.secondary">This is where you can create proposals and quotations.</Typography>
  </Box>
);

export default ProposalBuilder; 